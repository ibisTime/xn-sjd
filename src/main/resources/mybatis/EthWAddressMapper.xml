<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ogc.standard.dao.IEthWAddressDAO">
	<resultMap id="ethWAddress_DB_Result" type="ethWAddress">
		<result column="id" property="id" />
		<result column="address" property="address" />
		<result column="status" property="status" />
		<result column="create_datetime" property="createDatetime" />
		<result column="abandon_datetime" property="abandonDatetime" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id != null and id != '' ">
				AND t.id = #{id}
			</if>
			<if test="address != null and address != '' ">
				AND t.address = #{address}
			</if>
			<if test="addressForQuery != null and addressForQuery != '' ">
				AND t.address like "%"#{addressForQuery}"%"
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
			<if test="statusList != null and statusList != ''">
                AND `status` in
                <foreach item="item" index="index" collection="statusList" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
		</trim>
	</sql>
	<select id="select_ethWAddress" parameterType="ethWAddress" resultMap="ethWAddress_DB_Result">
		SELECT
		t.id
		,t.address
		,t.status
		,t.create_datetime
		,t.abandon_datetime
		FROM
		tcoin_eth_waddress t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>
	
	<select id="select_ethWAddress_count" parameterType="ethWAddress"
		resultType="java.lang.Long">
		SELECT count(1) FROM tcoin_eth_waddress t
		<include refid="where_condition" />
	</select>
	
	<insert id="insert_ethWAddress" parameterType="ethWAddress">
		INSERT INTO
		tcoin_eth_waddress(
		
		address
		,status
		,create_datetime
		
		)
		VALUES(
		
		#{address}
		,#{status}
		,#{createDatetime}
		
		)
	</insert>

    <update id="update_abandon" parameterType="ethWAddress">
		UPDATE tcoin_eth_waddress
		SET
		status = #{status}
		,abandon_datetime = #{abandonDatetime}
		WHERE id = #{id}
	</update>

</mapper>