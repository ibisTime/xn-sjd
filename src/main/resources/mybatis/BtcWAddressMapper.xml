<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ogc.standard.dao.IBtcWAddressDAO">
	<resultMap id="btcWAddress_DB_Result" type="btcWAddress">
		<result column="id" property="id" />
		<result column="address" property="address" />
		<result column="status" property="status" />
		<result column="create_datetime" property="createDatetime" />
	</resultMap>
	<sql id="where_condition">
		<trim prefix="WHERE" prefixOverrides="AND | OR">
			<if test="id != null and id != '' ">
				AND t.id = #{id}
			</if>
			<if test="address != null and address != '' ">
				AND t.address = #{address}
			</if>
			<if test="addressForQuery != null and addressForQuery != '' ">
				AND t.address like "%"#{addressForQuery}"%"
			</if>
			<if test="status != null and status != '' ">
				AND t.status = #{status}
			</if>
			<if test="statusList != null and statusList != ''">
                AND `status` in
                <foreach item="item" index="index" collection="statusList" open="(" separator="," close=")">
                    #{item}
                </foreach>
            </if>
		</trim>
	</sql>
	<select id="select_btcWAddress" parameterType="btcWAddress" resultMap="btcWAddress_DB_Result">
		SELECT
		t.id
		,t.address
		,t.status
		,t.create_datetime
		FROM
		tcoin_btc_waddress t
		<include refid="where_condition" />
		<trim prefix="ORDER BY ">
			<if test="order != null and order != '' ">
				t.${order}
			</if>
		</trim>
	</select>
	
	<select id="select_btcWAddress_count" parameterType="btcWAddress"
		resultType="java.lang.Long">
		SELECT count(1) FROM tcoin_btc_waddress t
		<include refid="where_condition" />
	</select>
	
	<insert id="insert_btcWAddress" parameterType="btcWAddress">
		INSERT INTO
		tcoin_btc_waddress(
		
		address
		,status
		,create_datetime
		
		)
		VALUES(
		
		#{address}
		,#{status}
		,#{createDatetime}
		
		)
	</insert>

    <update id="update_abandon" parameterType="btcWAddress">
		UPDATE tcoin_btc_waddress
		SET
		status = #{status}
		WHERE id = #{id}
	</update>

</mapper>