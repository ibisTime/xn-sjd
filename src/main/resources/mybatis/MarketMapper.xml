<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ogc.standard.dao.IMarketDAO">

  <resultMap id="BaseResultMap" type="com.ogc.standard.domain.Market">
    <id column="id"  property="id" />
    <result column="symbol" jdbcType="VARCHAR" property="symbol" />
    <result column="refer_currency" jdbcType="VARCHAR" property="referCurrency" />
    <result column="origin" jdbcType="VARCHAR" property="origin" />
    <result column="last_price" jdbcType="DECIMAL" property="lastPrice" />
    <result column="bid" jdbcType="DECIMAL" property="bid" />
    <result column="ask" jdbcType="DECIMAL" property="ask" />
    <result column="mid" jdbcType="DECIMAL" property="mid" />
    <result column="low" jdbcType="DECIMAL" property="low" />
    <result column="high" jdbcType="DECIMAL" property="high" />
    <result column="volume" jdbcType="VARCHAR" property="volume" />
    <result column="update_datetime" jdbcType="TIMESTAMP" property="updateDatetime" />
    <result column="coinmarketcap_id" jdbcType="VARCHAR" property="coinmarketcapId" />
  </resultMap>

  <sql id="Base_Column_List">
    id, symbol, refer_currency, origin, last_price, bid, ask, mid, low, high, volume,
    update_datetime, coinmarketcap_id
  </sql>

  <sql id="whereCondation">
    <trim prefix="where" prefixOverrides="AND | OR">
      <if test="symbol != null and symbol != ''">
        AND symbol = #{symbol}
      </if>
      <if test="referCurrency != null and referCurrency != ''">
        AND refer_currency = #{referCurrency}
      </if>
    </trim>
  </sql>

  <update id="update" parameterType="com.ogc.standard.domain.Market">
  	INSERT INTO
  	tcoin_market(
  	symbol
  	,refer_currency
  	,origin
  	,last_price
  	,bid
  	,ask
  	,mid
  	,low
  	,high
  	,volume
  	,update_datetime
  	,coinmarketcap_id
  	)
  	VALUE(
  	#{symbol,jdbcType=VARCHAR}
  	 ,#{referCurrency,jdbcType=VARCHAR}
  	 ,#{origin,jdbcType=VARCHAR}
  	 ,#{lastPrice,jdbcType=DECIMAL}
  	 ,#{bid,jdbcType=DECIMAL}
  	 ,#{ask,jdbcType=DECIMAL}
  	 ,#{mid,jdbcType=DECIMAL}
  	 ,#{low,jdbcType=DECIMAL}
  	 ,#{high,jdbcType=DECIMAL}
  	 ,#{volume,jdbcType=VARCHAR}
  	 ,#{updateDatetime,jdbcType=TIMESTAMP}
  	 ,#{coinmarketcapId,jdbcType=VARCHAR}
  	)
   	on DUPLICATE key update
   	symbol = #{symbol,jdbcType=VARCHAR},
    refer_currency = #{referCurrency,jdbcType=VARCHAR},
    origin = #{origin,jdbcType=VARCHAR},
    last_price = #{lastPrice,jdbcType=DECIMAL},
    bid = #{bid,jdbcType=DECIMAL},
    ask = #{ask,jdbcType=DECIMAL},
    mid = #{mid,jdbcType=DECIMAL},
    low = #{low,jdbcType=DECIMAL},
    high = #{high,jdbcType=DECIMAL},
    volume = #{volume,jdbcType=VARCHAR},
    update_datetime = #{updateDatetime,jdbcType=TIMESTAMP},
	coinmarketcap_id = #{coinmarketcapId,jdbcType=VARCHAR}
  </update>

  <select id="selectedByCoinTypeAndOrigin" parameterType="com.ogc.standard.domain.Market" resultMap="BaseResultMap" >
    select
    <include refid="Base_Column_List" />
    from tcoin_market
    <include refid="whereCondation"/>
  </select>

  <select id="selectTotalCount" parameterType="com.ogc.standard.domain.Market"
          resultType="java.lang.Long">
    SELECT count(1) FROM tcoin_arbitrate t
    <include refid="whereCondation" />
  </select>

  <select id="select" parameterType="java.lang.String" resultMap="BaseResultMap">
    select
    <include refid="Base_Column_List" />
    from tcoin_market
    <include refid="whereCondation"/>
  </select>

  <select id="selectMarketAvg" resultType="Decimal" parameterType="com.ogc.standard.domain.Market">
    SELECT avg(mid)
    FROM tcoin_market
    WHERE symbol = #{symbol}
  </select>

  <!--<select id="selectList" parameterType="java.lang.String" resultMap="BaseResultMap">-->
    <!--select-->
    <!--<include refid="Base_Column_List" />-->
    <!--from tcoin_market-->
    <!--<include refid="whereCondation"/>-->
  <!--</select>-->

</mapper>